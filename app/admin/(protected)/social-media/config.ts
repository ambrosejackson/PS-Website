/** Limits for the FOLLOW US strip (D-064). Shared by server actions and client UI. */
export const SOCIAL_MAX_ACTIVE = 50; // hard cap on active images
export const SOCIAL_SOFT_MIN = 6; // below this the marquee visibly repeats
export const SOCIAL_VIDEO_MAX_SECONDS = 15; // D-068: strip clips are short, muted, looped
